#include "lifo_fifo.h"
/**
 *execute - Takes the pointer to a function and execute it
 *Return: Nothing
 */
void execute(char *reception)
{
	int sec;
	int oper;
	int exe;

	if (reception[0])
	{
		oper = get_verifier(reception[0]);

		if (reception[1])
		{
			sec = atoi(reception[1]);
			   /*addnode with sec*/
		}
		exe = (*f)(
	}

}
